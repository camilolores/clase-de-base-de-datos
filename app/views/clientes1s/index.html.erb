<p id="notice"><%= notice %></p>

<h1>Clientes1s</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @clientes1s.each do |clientes1| %>
      <tr>
        <td><%= clientes1.name %></td>
        <td><%= clientes1.email %></td>
        <td><%= link_to 'Show', clientes1 %></td>
        <td><%= link_to 'Edit', edit_clientes1_path(clientes1) %></td>
        <td><%= link_to 'Destroy', clientes1, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Clientes1', new_clientes1_path %>
