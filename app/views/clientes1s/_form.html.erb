<%= form_with(model: clientes1, local: true) do |form| %>
  <% if clientes1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(clientes1.errors.count, "error") %> prohibited this clientes1 from being saved:</h2>

      <ul>
      <% clientes1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
